[tool:pytest]
testpaths = step_definitions
python_files = *_steps.py
python_functions = test_*
addopts = 
    --strict-markers
    --html=reports/report.html
    --self-contained-html
    -v
markers =
    smoke: Smoke tests
    regression: Regression tests
    login: Login related tests
    forms: Forms related tests